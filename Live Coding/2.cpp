#include <iostream>
using namespace std;

bool faktorial(int data, int n, int &index)
{
    for (int i = 0; i < n; i++)
    {
        for (int j = 0; i < n; i++)
        {
            int n = n * i++;
            if (int hasil = n / n - (n-1) * i++);
        }
    }

    return 0;
}

int main()
{
    int n = 5;
    int hasil;
    cout << hasil << "faktorial dari" << n << endl;

    return 0;
}